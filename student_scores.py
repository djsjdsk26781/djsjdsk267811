def add_student_score(student_scores, name, score):
    """
    Add a student's score to the dictionary of student scores.
    
    Args:
        student_scores (dict): Dictionary containing student scores.
        name (str): Name of the student.
        score (float): Score of the student.
    """
    student_scores[name] = score

def find_student_score(student_sc
